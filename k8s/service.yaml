apiVersion: v1
kind: Service
metadata:
  name: ai-devops-service
spec:
  type: NodePort
  selector:
    app: ai-devops-app
  ports:
    - protocol: TCP
      port: 5000          # Service port
      targetPort: 5000    # Container port
      nodePort: 30007